<UserControl
    x:Class="WpfApp.View.AuthView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WpfApp.View"
    Width="400"
    Height="300"
    Background="Snow">

    <GroupBox FontSize="16" Header="Authentication">
        <StackPanel Margin="20">
            <TextBlock HorizontalAlignment="Center" Text="Enter Password:" />
            <PasswordBox
                Width="200"
                Margin="8"
                PasswordChanged="PasswordBox_PasswordChanged" />
            <Button
                Width="100"
                Margin="0,20,0,0"
                Command="{Binding LoginCommand}"
                Content="Submit" />
        </StackPanel>
    </GroupBox>
</UserControl>
